# CRM on Studio

Read [AGENTS.md](./AGENTS.md) before working in `crm_studio/`. It covers the source-of-truth
rule (this folder is hand-edited and re-imported on `bench migrate`), the `@app/*` alias, the
code conventions — including when a comment is warranted — and the page-script constraints.
